format = "$battery$container$localip$git_branch$git_status$git_state[](#007ACC)$username$hostname[  ](fg:black bg:#007ACC)[$directory](bg:#007ACC)[](#007ACC)$status$character"
add_newline = false

[status]
disabled = false
style = "fg:bright-white bg:bright-red"
format = ' [](bright-red)[$int!]($style)[](bright-red) '

[[battery.display]]
threshold = 40

[username]
disabled = true
show_always = true
style_user = "fg:black bg:#007ACC"
style_root = "fg:black bg:red"
format ="[$user]($style)"

[hostname]
ssh_only = false
format = "[$hostname]($style)"
style = "fg:white bg:#007ACC"
trim_at = "."
disabled = false

[localip]
ssh_only = true
disabled = false
#style = "fg:black bg:bright-yellow"
#format = "[](bright-yellow)[$localipv4]($style)[](bright-yellow) "
format = "🌐 "

[container]
symbol = "🎁"
format = "[$symbol ](style)"

[directory]
truncation_length = 80
truncation_symbol = "…/"
style = "fg:white bg:#007ACC"
read_only_style = "bg:#007ACC"
format = "[$path]($style)[$read_only]($read_only_style)"

[git_branch]
style = "bg:purple fg:bright-white"
format = "[](purple)[$symbol$branch]($style)[](purple)"

[git_status]
# style = "bg:bright-red fg:bright-white"
# format = "([](bright-red)[$all_status$ahead_behind]($style)[](bright-red))"
format = "( [$all_status$ahead_behind]($style) )"
ahead = "⇡${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
behind = "⇣${count}"
